<!DOCTYPE html>
html(lang="en")
head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    meta(http-equiv="X-UA-Compatible", content="ie=edge")
    title Document
    script(src="https://cdn.bootcss.com/angular.js/1.3.14/angular.min.js")
    script(src="https://cdn.bootcss.com/angular.js/1.3.14/angular-resource.min.js")
    script(src="js/admin.js")
    link(rel="stylesheet", href="https://cdn.bootcss.com/bootstrap/3.0.1/css/bootstrap.min.css")
    link(rel="stylesheet", href="css/admin.css")
body(ng-app="adminApp")
    .container(ng-controller="querycontroller")
        ul#post
            li(ng-repeat="post in items")
               .jumbotron.clearfix
                    dl
                        dt 标题:{{post.name}}
                        dt 图片:
                            img(ng-src="http://localhost:3000/images/{{post.image}}")
                    dl
                        dt
                            a.btn.btn-danger.btn-lg(
                                ng-click="delete(post)"
                                role="button"
                            ) 删除
            
            nav
                ul.pagination
                    li
                        a(ng-click="Previous()")
                            span 上一页
                    li(
                        ng-repeat="page in pageList"
                        ng-class="{active: isActivePage(page)}"
                      )
                        a(ng-click="selectPage(page)") {{page}}
                    li
                        a(ng-click="Next()")
                            span 下一页