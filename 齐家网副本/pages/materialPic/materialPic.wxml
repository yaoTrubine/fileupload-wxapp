<!--materialPic.wxml-->
<view class="materialPic">
<block wx:for="{{images}}" wx:for-item='item' wx:key='item.id'>
  <image src="https://qzw.flhome.cn/images/vendors/{{item}}" data-id="{{item._id}}" data-name="{{item.name}}" data-src="{{item}}" bindtap="vendorDetail"></image>
</block>

</view>
